@model PeteFest.Web.Models.Festival.TicketsModel

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container">
    <section id="tickets">
        <div class="col-sm-10 col-sm-offset-1 scrollimation fade-right add-padding">
            <div class="scrollimation fade-up d1">
                <h1 class="big-text text-center">@Model.HeaderText</h1>
                <p class="lead">@Model.ParagraphText</p>
            </div>
        </div>
    </section>
</div>
